import Vuex from "vuex";
import Vue from "vue";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    // data like
  },

  getters: {
    // computed properties like (Filter or Counting something)
  },

  actions: {
    // method like
  },

  mutations: {
    // Updating the state
  },
});
